# Minimal requirements for FastAPI deployment with webhooks and bot
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-dotenv==1.0.0
psycopg2-binary==2.9.9
# HTTP client for sending messages
requests==2.31.0
# AI Integration (optional - comment out if no API key)
google-generativeai==0.8.3